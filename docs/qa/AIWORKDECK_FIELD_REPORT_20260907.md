# 2026-09-07 实测问题逐项回写

依据：用户更新后的 aiworkdeck-bugs-20260907_1.md。版本目标 v0.36.0，资源包 1.1.1。本文区分本轮修复、既有修复、未复现及能力建议，不将单测等同于原业务场景实测。

| 问题 | 状态与处理 | 复测要点 |
|---|---|---|
| A1 核查写坏原底稿、重复插入 | 已有修复（v0.35）：分析输出目标约束、长文去重与写入预算；本轮保留。提示约束仍需按原话复测模型行为。 | 打开底稿发核查指令，检查原文未变、报告没有重复。 |
| A2 新建文档位置错误、空白却成功 | 已修：保留 v0.35 的目标文件夹参数和失败上报；补齐末尾表格 flush 失败上报。 | 指定子目录生成文档；写入失败必须明确报告。 |
| A3 手机 JPG/M4A 打不开 | 已有修复（v0.35）：按实际扩展名归一化类型。 | 双击手机同步的照片和录音。 |
| A4 时间轴退出码 2 | 已修：旧资源包缺模块时先提示更新，错误保留 stderr；资源包 1.1.1 已签名并同步两站。 | 先更新诉讼可视化资源包，再重试时间轴；语义地图回退保留。 |
| A5 静音转写泄露 JSON、扣费不明 | 已修：合法静音结果显示无人声；异常不展示原始 JSON；平台可能计费与重试提示明确。未改变供应商计费或办理退款。 | 用无声输入短录音，检查无人声提示、设备提醒和费用说明。 |
| A6 保存中/失败循环 | 已修：失败暂停自动重试，允许手动重试；关闭/离页保留未保存修改，包括保存期间继续输入。 | 制造保存失败，重试、继续编辑、关闭或切项目。 |
| A7 纪要表格散行 | 已修：生成文件引用了缺失的表格段落样式，LOWA导入时将内容移出单元格；补齐样式后原纪要恢复副本18格内容与标题位置均保留。另验17×7大表119格及边框。 | 检查原纪要待办表格及新生成纪要。 |
| B1 重启后剪贴板不采集、删除无效 | 采集已修：启动/旧窗关闭竞争清掉主窗口引用。删除无独立新根因；真实桌面复制、卡片显示、确认删除及数据库消失已验证。 | 重启后外部/编辑器复制，再确认删除一条新记录。 |
| B2 股权结构第一次不出图 | 已有修复（v0.35）：末位完成条件要求确认或产出图；保留人工确认。 | 首次任务应进入确认或出图，不应只给普通表格结束。 |
| B3 Word 全篇校对长时间无反馈 | 已有修复（v0.35）：批处理与分块进度；Office单测回归。未把单测视为 Word for Mac 原稿实测。 | 含既有修订/链接的五页文档做全文校对，观察进度和结束状态。 |
| B4 30步中断、重复目录 | 已修：保留批量移动/目录去重，补整表一次提交的中英指引；大表流式写入预算与建表对齐。不放大全局步数。 | 整理14份文件并生成17×7证据表，检查重复节点。 |
| B6 确认卡展开无响应 | 未复现：真实流解析器分片、漏闭合及响应式更新均通过；当前问题卡自身无折叠开关。 | 用原可视化确认流程复测，保留具体会话供继续定位。 |
| B7 回答插入变成竖线文本 | 已修：复用富文本写入，一条命令完成尾表，禁止混入另一轮Agent流。 | 插入含标题、粗体、7列表格的回答。 |
| B8 麦克风默认错误 | 已修：每次进入使用系统default，不再恢复旧iPhone设备；设备变动更新列表。 | 切系统输入后进入录音，再插拔设备。 |
| B9 转写利用案卷热词/纠错 | 不修（本轮能力建议）：需要转写网关热词协议与可审计纠错设计；保留原始转写，不把猜测改成证据。 | 后续独立设计；现有纪要纠错与不确定性标注保留。 |
| B10 偶发自动全屏尺寸 | 未复现：没有找到运行期间主动setBounds/setSize路径；主窗创建竞争与面板越界分别已修，不能认定是这次单发事件的根因。 | 外接屏/合盖/重新激活后观察窗口边界。 |
| B11 换风格覆盖手改 | 已修：每次明确确认，服务端写权限与确认门禁；排空在途保存后重画，打开画布自动刷新。 | 先取消确认（零修改），再确认并检查画布同步。 |
| B12 PNG 只有形状无文字 | 已修：Batik使用SVG文字后备；失败保留旧PNG。原图副本20,466→82,962字节，中文可见。 | 修改并保存图，检查同目录PNG文字。 |
| B13 去不掉项目符号 | 已修：清列表指引和真实读回；同次设置正文样式与居中时先切样式再应用对齐。 | 图注去项目符号、设正文并居中，检查正文不丢。 |
| C1 宽表格截断 | 已有修复（v0.35）：表格独立横向滚动。 | 并排打开底稿/批注与AI面板查看宽表。 |
| C2/C7 面板撑出窗口 | 已修：保留拖拽容器边界，窗口缩小/恢复旧布局也回夹，保留编辑区；忽略小于最小布局的瞬时尺寸，避免栏宽归零。实际窗口低于最小总宽时可能暂时裁切，恢复后回夹。 | 拖宽两侧面板后缩小窗口。 |
| C3 修订/批注计数不刷新 | 已有修复（v0.35）：文档修改通知刷新；本轮回答插入也发送该通知。 | Agent写入后检查计数。 |
| C4 iOS版在Mac设置卡住 | 已有移动端修复：原修复已在TestFlight 1.0.1(20)，本轮不改手机端源码、不另发手机包。 | 使用该构建或更新版本复测设置。 |
| C5 手机相机切换后黑屏 | 已有移动端修复：相机/录音会话切换收口已随TestFlight 1.0.1(20)。 | 连续切换照片/录像/录音。 |
| C6 上传残影、Office锁文件 | 已有修复（v0.35）：清理失败上传UI；过滤~$锁文件。 | 上传失败后关闭项目重进，打开Office时查看树。 |
| C8 语音栏难拖宽 | 已修：共用左栏手柄完整置于可点击范围内，并有拖动提示。 | 在语音栏右边缘拖宽阅读区。 |
| C9 录音改名回车无效 | 已修：两个改名入口Enter提交。 | 输入新标题按回车。 |
| C10 保存中关不了标签 | 已修：等待有预算；失败时默认保留，可明确放弃并关闭，迟到导出不能覆盖旧文件。 | 保存卡住时关闭，分别选择保留和放弃。 |
| C11 新docx兼容模式 | 已修：新文件设置现代兼容级别，LOWA导出不再降级；旧12、新15均真导出保留。 | Word打开新文件不显示兼容模式；旧文件保持原级别。 |
| C12 PNG默认名丢图名 | 已修默认名；原生导出仍允许用户选择Device位置。普通保存已有项目同目录PNG路径。 | 导出默认名含图名；项目内用普通保存。 |
| C13 关系箭头无说明 | 已修：旧title/subtitle/accent字段做确定性兼容映射，不改原地图；12/12边文字保留。 | 更新资源包后确认重画旧关系图，检查每条边说明。 |

详细图形验证见 [诉讼可视化记录](LITIGATION_FIELD_REPORT_20260907.md)。

## 发布与最终验证

- 后端完整验证：3,286 项，0 失败、10 项既有跳过；最终 Linux 包构建通过。
- 工作台单测：414 项通过；录音/插入/图形等定向回归通过；事件、语言与导航检查通过。H5 与编辑器包装构建通过。
- 桌面单测：95 项通过、1 项既有跳过。Office 单测：307 项通过。
- 全应用：141 步通过、0 失败，覆盖真实文件上传、版本记录、分支冲突、三实例协作、英文界面和资源包安装。英文模型本轮未产生工具调用，过程卡工具名检查明确跳过；工作台、设置、AI 面板英文截图人工检查通过。
- 真实 LOWA：原有 541 项通过；新生成文档专项验证17×7表、富文本、流式失败恢复、列表清除、兼容级别12/15与纪要表格导入。追加标题/居中段首尾及中间插入，原文字与格式均保留；英文编辑器ooLocale=en-US已确认。
- 已知性能基线：150页、30表、20图的孤立导出中位数10.83秒，超过既有10秒阈值。旧/新导出过滤器同机对照中位数10.503/10.466秒，未观察到新过滤器回退；阈值未放宽，不能将此项称为通过。
- 实际旧纪要不会自动修复；从其最初正确文件生成了本地恢复副本，验证导入/导出18格文字均保留，原文件未覆盖。

- 真实桌面完整链路通过：系统复制采集、确认删除、浏览器标签保活、编辑器工具栏、插入、自动保存、拖拽关联及下载后内容核验。
- 国内/国际Office 0.36.0安装器与静态页已上线；两Mac签名、公证装订与spctl通过，两manifest校验通过，16项公网文件SHA一致；WPS仅北京更新。
- 北京/新加坡插件云后端与北京案件后端均已更新，备份旧件，三处新包SHA一致，启动ERROR=0，公网健康检查通过。
- 桌面v0.36.0完整安装包与镜像尚待CI双平台成功后发布；最终发布验收在开发看板#490记录。

未复现项不计为已修。
